name: CMake automatic build for multiple platforms

on:
    push:
        branches: ['dponyatov']
    pull_request:
        branches: ['dponyatov']

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - run: echo "Workflow works!"
          - run: ls -la
          - run: cmake --version
